---
execute: 
  freeze: false
---

# 부록 {.unnumbered .appendix}

## 자주 묻는 질문 (FAQs)

* 예제 코드를 실행할 때 오류 메시지가 나타나거나 다른 결과가 나옵니다. 어떻게 해결합니까?
  * 모든 패키지 로드를 포함하여 챕터의 모든 코드를 순서대로 실행했는지 확인하십시오.
  * 부록의 소프트웨어 요구 사항을 참조하여 다른 버전의 R 또는 특정 패키지를 사용하고 있는지 확인하십시오.
  
* RStudio를 사용하여 볼 때와 저장할 때 플롯이 찌그러지거나 늘어나거나 다르게 보이는 것을 어떻게 막을 수 있습니까?
  * @sec-camcorder 에서 이 문제에 대한 해결책을 살펴보세요!
  
* 각 챕터 끝에 있는 연습 문제를 완료했습니다. 정답을 확인할 수 있나요?
  * 연습 문제는 정해진 답이 있는 규범적인 질문이 아니라 의도적으로 개방형으로 남겨 두었습니다. 데이터 시각화에는 정답이 하나만 있는 경우가 거의 없으므로 솔루션이 제공되지 않습니다. 여러분이 *어떻게* 다른 솔루션을 설계하고 구현할 것인지 생각하고, 대신 정답을 확인하는 방법으로 다른 사람들의 피드백을 받는 것이 좋습니다.

## 소프트웨어 요구 사항 {.unnumbered}

```{r}
#| label: r-version
#| echo: false
r_ver <- getRversion()
```

이 책은 R 버전 `r r_ver`을 사용하여 빌드되었습니다. 이 책은 버전 번호 `r system("quarto --version", intern = TRUE)`을 사용하는 [Quarto](https://quarto.org/)로 빌드되었습니다. 프로젝트 환경은 [pixi](https://pixi.sh/)를 사용하여 관리됩니다. 이 책을 빌드하는 데 필요한 모든 R 패키지는 다음 표에서 찾을 수 있습니다. 이 표에는 예제에 필요한 패키지뿐만 아니라 책을 만드는 데 필요한 모든 패키지가 포함되어 있습니다.

```{r}
#| label: tbl-package-versions
#| tbl-cap: "R 패키지 및 버전 번호"
#| echo: false
options(knitr.kable.NA = "")
ip <- installed.packages()
pkgs <- ip[, "Package"]
pkg_vs <- ip[, "Version"]
nr <- ceiling(length(pkgs) / 3)
pkg_df <- tibble::tibble(
  Package = pkgs,
  Version = pkg_vs
)
fill_NA <- as.data.frame(matrix(rep(c(NA, NA), times = nr * 3 - length(pkgs)), ncol = 2))
colnames(fill_NA) <- c("Package", "Version")
padded_pkgs <- rbind(pkg_df[(2 * nr + 1):length(pkgs), ], fill_NA)
new_pkg_df <-
  cbind(pkg_df[1:nr, ], pkg_df[(nr + 1):(2 * nr), ], padded_pkgs)
kableExtra::kbl(
  new_pkg_df,
  longtable = TRUE
) |>
  kableExtra::kable_styling(font_size = 7.5)
```

소스 코드의 사본은 {{< monolink "https://github.com/nrennie/art-of-viz" "github.com/nrennie/art-of-viz" >}}에서 찾을 수 있습니다.

## 데이터 {.unnumbered}

이 책에서 사용된 모든 데이터 세트 및 관련 라이선스에 대한 링크:

```{r}
#| label: data-sources
#| echo: false
#| output: asis
data_data <- readr::read_csv("data/data.csv") |>
  tidyr::drop_na()
for (i in seq_len(nrow(data_data))) {
  pander::pandoc.list(
    list(
      glue::glue("**Chapter {data_data$Chapter[i]}**: {data_data$Data[i]} {data_data$BibTex[i]}"),
      list(
        paste0(
          "Source: {{< monolink '",
          data_data$URL[i],
          "' '",
          stringr::str_remove(data_data$URL[i], "https://"),
          "' >}} "
        ),
        glue::glue("License: {data_data$License[i]}")
      )
    )
  )
}
```

## 이미지 {.unnumbered}

이 책에서 사용된 모든 이미지(저자가 만들지 않은 것) 및 관련 라이선스에 대한 링크:

```{r}
#| label: img-sources
#| echo: false
#| output: asis
img_data <- readr::read_csv("images/data.csv") |>
  tidyr::drop_na()
for (i in seq_len(nrow(img_data))) {
  pander::pandoc.list(
    list(
      glue::glue("Image: {img_data$Photographer[i]}"),
      list(
        paste0(
          "Source: {{< monolink '",
          img_data$Source[i],
          "' '",
          stringr::str_remove(img_data$Source[i], "https://"),
          "' >}} ",
          img_data$BibTex[i]
        ),
        glue::glue("License: {img_data$License[i]}")
      )
    )
  )
}
```

::: {.content-visible when-format="html"}

## 시각화 갤러리 {.unnumbered}

@sec-programming 장:

![](images/final/programming-languages.png){fig-align="center" fig-alt="각각 #, //, ;를 주석 기호로 사용하는 상위 10개 언어에 대해 언어 출현과 마지막 활동 사이의 시간을 보여주는 패면 덤벨 차트로, 언어와 순위를 나타내는 레이블이 추가되었습니다."}

@sec-museums 장:

![](images/final/museums.png){fig-align="center" fig-alt="10개 범주가 각각 강조 표시된 작은 다중 선형 차트의 스타일 버전입니다. 차트에는 사용자 정의 글꼴, 컬러 배경 및 멋진 간격이 있습니다."}


@sec-bees 장:

![](images/final/bee-colonies.png){fig-align="center" fig-alt="각 그리드 사각형이 2015-2020년 사이의 연도를 나타내고 캘리포니아, 노스다코타, 플로리다, 텍사스, 아이다호 또는 사우스다코타 중 하나를 나타내는 6x6 그리드. 각 사각형 내에서 점의 밀도는 벌 집단 수를 나타냅니다. 디자인은 훨씬 더 미니멀하며 그리드 사각형의 검은색 배경으로 인해 대비가 높아 노란색 점이 더 쉽게 보입니다."}


@sec-longbeach 장:

![](images/final/animal-intakes.png){fig-align="center" fig-alt="다양한 동물 유형에 대해 2017년과 2023년의 동물 포기 수를 보여주는 게이지 차트의 최종 스타일링."}


@sec-turbines 장:

![](images/final/wind-turbines.png){fig-align="center" fig-alt="각 지역의 기간별 터빈 수를 보여주는 픽토그램으로, 부제목의 색상 텍스트는 각 색상이 매핑되는 지역을 나타냅니다."}


@sec-cats 장:

![](images/final/cats.png){fig-align="center" fig-alt="반려묘의 나이와 실내에서 보내는 평균 시간 사이의 관계에 대한 버블 플롯. 텍스트 서식이 올바르게 지정되었으며 범례가 플롯과 범례 사이에 명확한 경계가 있는 인셋으로 나타납니다."}

@sec-nobel 장:

![](images/final/nobel-laureates.png){fig-align="center" fig-alt="시계 반대 방향으로 배열된 노벨 물리학상 수상자의 이름으로, 여성 수상자는 더 어두운 색상으로 강조 표시됩니다."}

@sec-lemurs 장:

![](images/final/lemurs.png){fig-align="center" fig-alt="붉은 목도리 여우원숭이의 체중이 나이에 따라 어떻게 변하는지 보여주는 산점도로, 오른쪽 하단 모서리에 붉은 목도리 여우원숭이의 이미지가 오버레이되어 있습니다."}

@sec-r-pkgs 장:

![](images/final/r-packages.png){fig-align="center" fig-alt="8개의 핵심 tidyverse 패키지 각각에 대한 시간 경과에 따른 패키지 사용량을 보여주는 패면 영역 차트. 육각형 스티커가 패면의 텍스트 레이블을 대체했습니다."}
@sec-doctors 장:

![](images/final/doctors.png){fig-align="center" fig-alt="파란색 배너 안에 제목이 있고 페이지 오른쪽 하단에 수평 컬러바 범례가 있는 세계 지도를 보여주는 최종 스타일 지정된 플롯."}

@sec-time-zones 장:

![](images/final/time-zones.png){fig-align="center" fig-alt="세계 지도에서 서로 다른 시간대 위치를 나타내는 점으로, 점은 지리적 지역별로 색상이 지정됩니다. 제목과 부제목은 왼쪽 하단에 표시되며, 오른쪽 하단에 표시된 인셋 막대 차트가 범례로 사용됩니다."}
@sec-house 장:

![](images/final/us-elections.png){fig-align="center" fig-alt="시간 경과에 따른 정당별 득표율을 보여주는 미국 모양의 그리드로 배열된 패면 영역 차트. 캘리포니아에 대한 영역 차트의 확대 버전이 왼쪽에서 범례로 사용됩니다."}

## 책 표지 {.unnumbered}

이 책의 (작업 중인) 표지도 **`ggplot2`**로 만들어졌습니다. 아래에서 코드를 볼 수 있습니다.

```{r}
#| file: R/cover.R
#| echo: true
#| eval: true
#| code-fold: true
#| fig-asp: 1.5
#| fig.show: hold
```

:::
